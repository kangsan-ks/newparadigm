<IfModule mod_rewrite.c>
RewriteEngine on
RewriteBase /
RewriteCond $1 !^(public)/
RewriteCond %{HTTP_HOST} ^(www.)?caufineartexhibition.kr [NC]
RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>

<IfModule mod_rewrite.c>
RewriteEngine on
RewriteBase /
RewriteCond $1 !^(public)/
RewriteCond %{HTTP_HOST} ^(www.)?cau2020.gabia.io [NC]
RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>